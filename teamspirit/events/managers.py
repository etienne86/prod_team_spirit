"""Contain the managers for the models in app ``events``."""

from django.db import models

# from django.utils.translation import ugettext_lazy as _


class EventManager(models.Manager):
    """Manage the model ``Event``."""
    pass
